#include <stdio.h>
int main(){
    int a,b ;
    scanf("%d %d",&a, &b);
    if (a>0 && a=0) {
        printf("True", a);

        else if {
            printf("False",a);
        }
    else if (b<0 && b=0 ){
        printf("True", b)
        {
            else if {
                printf("False",b);
            }
        }

    }

    }