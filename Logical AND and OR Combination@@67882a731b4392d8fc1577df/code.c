#include <stdio.h>
int main(){
    int a,b ;
    scanf("%d %d",&b &a);
    if (a>0 && a=0) {
        printf("True", a);

        else {
            printf("False",b);
        }
    else if (b<0 && b=0 ){
        printf("True", b)
        {
            else {
                printf("False");
            }
        }

    }

    }}